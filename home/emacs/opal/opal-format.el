;;; opal-format.el --- formatter -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(require 'opal-package)

(use-package apheleia
  :config
  (apheleia-global-mode +1))

(provide 'opal-format)
;;; opal-format.el ends here
